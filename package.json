{
  "name": "github_scraper",
  "version": "1.0.0",
  "description": "this page displays a list of linked repos for the username that the user defines",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Evans",
  "license": "ISC",
  "devDependencies": {
    "angular": "^1.5.5",
    "bootstrap": "^3.3.6",
    "jquery": "^2.2.3"
  }
}
